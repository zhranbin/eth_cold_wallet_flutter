import 'package:flutter/material.dart';

class ABImage extends StatelessWidget {
  const ABImage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
  //
  // ABImage.asset(String asset, {
  // }
  //

}
